# Build Environment Variables (CI/CD Template)
# Copy to .env.build and fill in actual values, OR set these in Vercel dashboard.
# This file is for documentation only and is NOT loaded automatically.

# Database - REQUIRED for prisma generate - must be set by CI system before build
# SQLite (local dev): DATABASE_URL="file:./dev.db"
# PostgreSQL (production): DATABASE_URL="postgresql://user:password@host:5432/opencarbox"
DATABASE_URL="postgresql://user:password@host:5432/opencarbox"

# Supabase - REQUIRED for app functionality
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional for build phase
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://opencarbox.vercel.app
