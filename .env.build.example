# Build Environment Variables (CI/CD)
# Template for GitHub Actions, Vercel, and other CI/CD pipelines
# Copy values and set them as environment variables in your CI/CD system.
# This file is NOT loaded automatically - it is documentation only.

# Database - REQUIRED for prisma generate - must be set by user/CI system before build
DATABASE_URL=postgresql://user:password@localhost:5432/db

# Supabase - REQUIRED for app functionality
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional for build phase
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://opencarbox.vercel.app
