# PRODUCTION - Vercel will inject these
# DATABASE_URL and DIRECT_URL must be set in Vercel dashboard
# Fallback for builds without DATABASE_URL
DATABASE_URL=postgresql://fallback:fallback@localhost/fallback?schema=public
NEXT_PUBLIC_APP_URL=https://opencarbox.vercel.app
NODE_ENV=production
