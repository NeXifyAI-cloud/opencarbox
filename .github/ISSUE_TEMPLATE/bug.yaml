name: "🐛 Bug Report"
description: "Report a bug or issue (DOS v1.1)"
title: "[BUG] "
labels: ["type:bug", "status:triage"]
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        # 🐛 Bug Report (DOS v1.1)
        
        Help us improve by reporting bugs. Please provide details to reproduce.
  
  - type: dropdown
    id: area
    attributes:
      label: "📍 Affected Area"
      description: "Where is the bug?"
      options:
        - "🛒 Shop (Carvantooo)"
        - "🔧 Werkstatt (Workshop)"
        - "🚗 Autohandel (Dealership)"
        - "🔐 Auth/Security"
        - "📊 Tracking/Events"
        - "🛠️ Backend/API"
        - "❓ Other"
    validations:
      required: true
  
  - type: dropdown
    id: severity
    attributes:
      label: "🚨 Severity"
      description: "How critical is this bug?"
      options:
        - "🔴 Critical (Site down, data loss)"
        - "🟠 High (Major feature broken, security issue)"
        - "🟡 Medium (Feature partially broken, user confused)"
        - "🟢 Low (Minor UX issue, cosmetic)"
    validations:
      required: true
  
  - type: textarea
    id: description
    attributes:
      label: "📝 Bug Description"
      description: "What is the bug? What should happen instead?"
      placeholder: "Clear description of the issue"
    validations:
      required: true
  
  - type: textarea
    id: reproduce
    attributes:
      label: "🔄 Steps to Reproduce"
      description: "Exact steps to reproduce the bug"
      placeholder: |
        1. Go to...
        2. Click on...
        3. Observe...
        4. Expected: ...
        5. Actual: ...
    validations:
      required: true
  
  - type: textarea
    id: environment
    attributes:
      label: "🖥️ Environment"
      description: "Browser, OS, etc."
      placeholder: |
        - Browser: Chrome 120
        - OS: macOS 14
        - Device: MacBook Pro
        - URL: https://opencarbox.vercel.app/...
    validations:
      required: false
  
  - type: textarea
    id: kpi_impact
    attributes:
      label: "📊 KPI Impact"
      description: "Which KPI does this bug affect?"
      placeholder: |
        - Metric: `conversion_rate` → Impact: -X%
        - Metric: `error_rate` → Impact: +X%
    validations:
      required: false
  
  - type: dropdown
    id: dsgvo
    attributes:
      label: "🔐 DSGVO/Security Impact (G10)"
      description: "Does this bug affect data privacy or security?"
      options:
        - "✅ No security/privacy impact"
        - "⚠️ Potential PII exposure"
        - "🚨 Confirmed data leak"
        - "🔒 Permission/RLS bypass"
        - "❓ Uncertain"
    validations:
      required: true
  
  - type: checkboxes
    id: tracking
    attributes:
      label: "📊 Tracking & Events (G4, G6)"
      description: "Does this affect tracking?"
      options:
        - label: "Bug affects event tracking accuracy"
          required: false
        - label: "Bug may leak PII in events"
          required: false
        - label: "Bug impacts KPI calculation"
          required: false
  
  - type: textarea
    id: context
    attributes:
      label: "🔗 Additional Context"
      description: "Error logs, screenshots, related issues, etc."
      placeholder: "Console errors, network logs, etc."
    validations:
      required: false
  
  - type: markdown
    attributes:
      value: |
        ---
        **Before you submit**:
        - [ ] I've searched existing issues (no duplicates)
        - [ ] I've tested in incognito/private mode
        - [ ] I can reliably reproduce the bug
        - [ ] I've included relevant error logs
        
        **OpenClaw Bot** will triage this issue automatically.
