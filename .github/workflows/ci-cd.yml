name: CI/CD (deprecated)

on:
  workflow_dispatch:

concurrency:
  group: ci-cd-deprecated-${{ github.ref }}
  cancel-in-progress: true

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deprecated: use ci.yml (validation) + auto-deploy.yml (deployment)."
