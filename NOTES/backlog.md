# Backlog

## Now
- [ ] Replace in-memory rate limit with distributed store (Upstash/Redis) for multi-instance deployment.
- [ ] Add Supabase auth helper for cookie-based Next.js session fallback in route handlers.
- [ ] Add integration tests for `/api/ai/chat` including 401/429/502 paths.

## Next
- [ ] Add docs site in MDX (`/docs`) with architecture and API pages.
- [ ] Add GitHub branch protection bootstrap via `gh api` using repo-level inputs.
- [ ] Add Vercel preview/prod deployment verification in setup script.
