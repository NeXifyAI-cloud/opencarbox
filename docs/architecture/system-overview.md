# System-Architektur - OpenCarBox & Carvantooo

> **ARCHITEKTUR-ÜBERSICHT**
> Dieses Dokument beschreibt die technische Architektur des Gesamtsystems.

---

## 🏗️ Architektur-Diagramm

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENTS                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐           │
│    │ Desktop  │    │  Mobile  │    │  Tablet  │    │   PWA    │           │
│    │ Browser  │    │  Browser │    │  Browser │    │   App    │           │
│    └────┬─────┘    └────┬─────┘    └────┬─────┘    └────┬─────┘           │
│         │               │               │               │                   │
│         └───────────────┴───────────────┴───────────────┘                   │
│                                   │                                         │
│                                   ▼                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                            VERCEL EDGE                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                   │                                         │
│    ┌──────────────────────────────┴──────────────────────────────┐         │
│    │                     NEXT.JS 14+ APP                          │         │
│    │                      (App Router)                            │         │
│    ├──────────────────────────────────────────────────────────────┤         │
│    │                                                              │         │
│    │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │         │
│    │  │  (shop)     │  │ (werkstatt) │  │ (autohandel)│          │         │
│    │  │ Carvantooo  │  │  OpenCarBox │  │  OpenCarBox │          │         │
│    │  │   Rot-Theme │  │  Blau-Theme │  │  Blau-Theme │          │         │
│    │  └─────────────┘  └─────────────┘  └─────────────┘          │         │
│    │                                                              │         │
│    │  ┌─────────────────────────────────────────────────────┐    │         │
│    │  │              SHARED COMPONENTS                       │    │         │
│    │  │  Header │ Footer │ Navigation │ ChatWidget          │    │         │
│    │  └─────────────────────────────────────────────────────┘    │         │
│    │                                                              │         │
│    │  ┌─────────────────────────────────────────────────────┐    │         │
│    │  │              API ROUTES / SERVER ACTIONS             │    │         │
│    │  │  /api/produkte │ /api/bestellungen │ /api/termine   │    │         │
│    │  └─────────────────────────────────────────────────────┘    │         │
│    │                                                              │         │
│    └──────────────────────────────────────────────────────────────┘         │
│                                   │                                         │
│                                   ▼                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                         EXTERNE SERVICES                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐           │
│  │  SUPABASE  │  │   STRIPE   │  │ MEILISEARCH│  │  RESEND    │           │
│  │ PostgreSQL │  │  Payments  │  │   Search   │  │   Email    │           │
│  │   + Auth   │  │  Webhooks  │  │   Index    │  │  Delivery  │           │
│  │  + Storage │  │            │  │            │  │            │           │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘           │
│                                                                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐           │
│  │  BOTPRESS  │  │  WHATSAPP  │  │  TECDOC    │  │ PREIS-     │           │
│  │  Chatbot   │  │  Business  │  │  Fahrzeug- │  │ VERGLEICH  │           │
│  │            │  │    API     │  │   Daten    │  │  Portale   │           │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Datenfluss

### Produktkauf-Flow

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Kunde   │────▶│ Warenkorb│────▶│ Checkout │────▶│  Stripe  │
│  sucht   │     │  füllen  │     │  starten │     │  Zahlung │
└──────────┘     └──────────┘     └──────────┘     └────┬─────┘
                                                        │
                                                        ▼
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Kunde   │◀────│ WhatsApp │◀────│ Rechnung │◀────│ Webhook  │
│  erhält  │     │  senden  │     │ erstellen│     │ empfangen│
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

### Terminbuchungs-Flow

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Kunde   │────▶│ Service  │────▶│ Fahrzeug │────▶│  Termin  │
│  wählt   │     │  wählen  │     │  angeben │     │  wählen  │
└──────────┘     └──────────┘     └──────────┘     └────┬─────┘
                                                        │
                                                        ▼
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Kunde   │◀────│  E-Mail  │◀────│ Termin in│◀────│ Kontakt- │
│  erhält  │     │ + WhatsApp│    │    DB    │     │  daten   │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

---

## 📦 Komponenten-Architektur

### Atomic Design Hierarchie

```
┌─────────────────────────────────────────────────────────────┐
│                        TEMPLATES                             │
│  ShopLayout │ ServiceLayout │ AdminLayout │ CheckoutLayout   │
├─────────────────────────────────────────────────────────────┤
│                        ORGANISMS                             │
│  Header │ Footer │ ProductGrid │ VehicleFinder │ ChatWidget │
├─────────────────────────────────────────────────────────────┤
│                        MOLECULES                             │
│  ProductCard │ SearchBar │ PriceDisplay │ FormField │ NavItem│
├─────────────────────────────────────────────────────────────┤
│                          ATOMS                               │
│  Button │ Input │ Badge │ Icon │ Avatar │ Spinner │ Skeleton │
└─────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Datenbank-Schema (Übersicht)

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│      USER       │       │     VEHICLE     │       │     PRODUCT     │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id              │       │ id              │       │ id              │
│ email           │◀──────│ userId          │       │ name            │
│ name            │       │ hsn             │       │ slug            │
│ phone           │       │ tsn             │       │ price           │
│ role            │       │ licensePlate    │       │ description     │
│ createdAt       │       │ brand           │       │ images          │
└─────────────────┘       │ model           │       │ stock           │
        │                 │ year            │       │ categoryId      │
        │                 └─────────────────┘       └────────┬────────┘
        │                         │                          │
        ▼                         ▼                          ▼
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│      ORDER      │       │   APPOINTMENT   │       │    CATEGORY     │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id              │       │ id              │       │ id              │
│ userId          │       │ userId          │       │ name            │
│ status          │       │ vehicleId       │       │ slug            │
│ total           │       │ serviceId       │       │ parentId        │
│ shippingAddress │       │ date            │       │ description     │
│ paymentIntent   │       │ time            │       └─────────────────┘
│ createdAt       │       │ status          │
└────────┬────────┘       │ notes           │
         │                └─────────────────┘
         ▼
┌─────────────────┐       ┌─────────────────┐
│   ORDER_ITEM    │       │     SERVICE     │
├─────────────────┤       ├─────────────────┤
│ id              │       │ id              │
│ orderId         │       │ name            │
│ productId       │       │ description     │
│ quantity        │       │ price           │
│ price           │       │ duration        │
└─────────────────┘       │ categoryId      │
                          └─────────────────┘
```

---

## 🔐 Sicherheitsarchitektur

### Authentifizierung

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Client    │────▶│  Supabase   │────▶│  JWT Token  │
│  (Browser)  │     │    Auth     │     │  (httpOnly) │
└─────────────┘     └─────────────┘     └─────────────┘
```

### API-Sicherheit

- **Rate Limiting:** Max. 100 Requests/Minute pro IP
- **CSRF Protection:** Token-basiert für Mutations
- **Input Validation:** Zod-Schemas auf Server-Seite
- **SQL Injection:** Prisma ORM (prepared statements)
- **XSS:** React escaping + CSP Headers

---

## 📊 Performance-Strategie

### Rendering-Strategien

| Seitentyp | Strategie | Revalidierung |
|-----------|-----------|---------------|
| Homepage | ISR | 60 Sekunden |
| Produktliste | ISR | 60 Sekunden |
| Produktdetail | ISR | 30 Sekunden |
| Warenkorb | CSR | - |
| Checkout | CSR | - |
| Admin | CSR | - |
| Service-Seiten | SSG | Build-Time |

### Caching

```
┌─────────────────────────────────────────────────────────────┐
│                      CACHING LAYERS                          │
├─────────────────────────────────────────────────────────────┤
│  1. Browser Cache (Cache-Control Headers)                    │
│  2. Vercel Edge Cache (CDN)                                 │
│  3. Next.js Data Cache (fetch cache)                        │
│  4. React Query Cache (Client-Side)                         │
│  5. Meilisearch Cache (Search Results)                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔌 Externe API-Integrationen

### Stripe

```typescript
// Zahlungsflow
1. createCheckoutSession() → Stripe Session
2. Redirect zu Stripe Checkout
3. Webhook: payment_intent.succeeded
4. Bestellung Status → "bezahlt"
5. E-Mail + WhatsApp Benachrichtigung
```

### Meilisearch

```typescript
// Produktsuche
1. Produkt erstellt/geändert → Index-Update
2. Kunde tippt Suchbegriff
3. Instant-Search Query an Meilisearch
4. Ergebnisse mit Typo-Toleranz
5. Facetten-Filter anwenden
```

### TecDoc/HSN-TSN

```typescript
// Fahrzeugsuche
1. Kunde gibt HSN/TSN ein
2. API-Call an TecDoc
3. Fahrzeugdaten zurück (Marke, Modell, etc.)
4. Passende Produkte filtern
5. "Meine Garage" speichern (optional)
```

---

## 📁 Ordnerstruktur (Detail)

```
src/
├── app/                          # Next.js App Router
│   ├── (marketing)/              # Marketing-Seiten
│   │   ├── page.tsx              # Homepage
│   │   ├── ueber-uns/
│   │   ├── kontakt/
│   │   └── layout.tsx
│   ├── (shop)/                   # Carvantooo Shop
│   │   ├── produkte/
│   │   │   ├── [slug]/
│   │   │   │   └── page.tsx      # Produktdetail
│   │   │   └── page.tsx          # Produktliste
│   │   ├── kategorie/
│   │   │   └── [slug]/
│   │   ├── warenkorb/
│   │   ├── checkout/
│   │   ├── bestellung/
│   │   │   └── [id]/
│   │   └── layout.tsx            # Shop Layout (Rot-Theme)
│   ├── (werkstatt)/              # OpenCarBox Werkstatt
│   │   ├── services/
│   │   │   └── [slug]/
│   │   ├── terminbuchung/
│   │   ├── preise/
│   │   └── layout.tsx            # Werkstatt Layout (Blau-Theme)
│   ├── (autohandel)/             # OpenCarBox Autohandel
│   │   ├── fahrzeuge/
│   │   │   └── [id]/
│   │   ├── finanzierung/
│   │   └── layout.tsx            # Autohandel Layout (Blau-Theme)
│   ├── admin/                    # Admin Panel
│   │   ├── dashboard/
│   │   ├── produkte/
│   │   ├── bestellungen/
│   │   ├── termine/
│   │   └── layout.tsx
│   ├── api/                      # API Routes
│   │   ├── stripe/
│   │   │   ├── checkout/
│   │   │   └── webhook/
│   │   ├── produkte/
│   │   ├── bestellungen/
│   │   ├── termine/
│   │   └── hsn-tsn/
│   ├── layout.tsx                # Root Layout
│   ├── globals.css               # Global Styles
│   └── not-found.tsx             # 404 Page
├── components/
│   ├── ui/                       # Basis UI (shadcn/ui)
│   ├── layout/                   # Header, Footer, etc.
│   ├── shared/                   # Übergreifend
│   ├── shop/                     # Shop-spezifisch
│   ├── werkstatt/                # Werkstatt-spezifisch
│   ├── autohandel/               # Autohandel-spezifisch
│   └── chat/                     # Chat/WhatsApp
├── hooks/                        # Custom Hooks
├── lib/                          # Utilities
├── stores/                       # Zustand Stores
└── types/                        # TypeScript Types
```

---

**Letzte Aktualisierung:** 2024-12-05

